export interface Product {
  id: number;
  name: string;
  price: number;
  description: string;
  stock: number;
}

export interface FormData {
  name: string;
  price: number;
  description: string;
  stock: number;
}
